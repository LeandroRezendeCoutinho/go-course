package functions

func Call_init() {
	// init
	// init is a special function that is called automatically when the program starts
	// it is used to initialize the variables
	// it is not possible to call the init function manually
	// it is not possible to call the init function from another function
	// it is not possible to call the init function from another package
	// it is not possible to call the init function from the main function
	// it is not possible to call the init function from the test function
	// it is not possible to call the init function from the benchmark function
	// it is not possible to call the init function from the example function
}
